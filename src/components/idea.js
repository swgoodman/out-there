import React, { Component } from 'react'

const Idea = () => {

  const { idea } = this.props;

      return (
        <div>
          <h1>{idea.header}</h1>
        </div>
      );

}

export default Idea;
